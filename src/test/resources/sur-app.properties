sur.mongodb.host=${sur.mongodb.host}
sur.mongodb.port=${sur.mongodb.port}
sur.mongodb.name=${sur.mongodb.name}
#sur.mongodb.user=${sur.mongodb.user}
#sur.mongodb.password=${sur.mongodb.password}

sur.mongodb.safe=${sur.mongodb.safe}
sur.mongodb.autoConnectRetry=${sur.mongodb.autoConnectRetry}
sur.mongodb.connectionsPerHost=${sur.mongodb.connectionsPerHost}
sur.mongodb.connectTimeout=${sur.mongodb.connectTimeout}
sur.mongodb.socketTimeout=${sur.mongodb.socketTimeout}

# if not set sur.mongodb.replicaSetSeeds, it will use host setup above.
sur.mongodb.replicaSetSeeds=${sur.mongodb.replicaSetSeeds}
sur.mongodb.slaveOk=${sur.mongodb.slaveOk}

sur.mophia.mapping=com.sohu.sur.model

# 0 means using default size (1000)
sur.lockPool.size=${sur.lockPool.size}

# cache configuration
sur.localCache.inuse=${sur.localCache.inuse}
sur.localCache.name=${sur.localCache.name}
sur.remoteCache.inuse=${sur.remoteCache.inuse}
sur.remoteCache.memcachedServers=${sur.remoteCache.memcachedServers}
sur.remoteCache.memcachedConnPoolSize=${sur.remoteCache.memcachedConnPoolSize}

# api uri
sur.api.changeBonus.uri=${sur.api.changeBonus.uri}
sur.api.changeActivity.uri=${sur.api.changeActivity.uri}

mall.itemGiftService.url=${mall.itemGiftService.url}
mall.itemLotteryGiftService.url=${mall.itemLotteryGiftService.url}
mall.logService.url=${mall.logService.url}
mall.recommend.url=${mall.recommend.url}